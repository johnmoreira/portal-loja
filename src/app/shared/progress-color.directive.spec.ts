import { ProgressColorDirective } from './progress-color.directive';

describe('ProgressColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressColorDirective();
    expect(directive).toBeTruthy();
  });
});
